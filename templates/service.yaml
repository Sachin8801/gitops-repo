apiVersion: v1
kind: Service
metadata:
  name: ecommerce-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: ecommerce-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: {{ .Values.service.port }}
